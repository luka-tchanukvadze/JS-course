let students = 21;

// students = students + 1;
console.log(students);

let extraStudents = students % 3;
console.log(extraStudents);

// students += 1;
// students -= 1;
// students *= 2;
// students /= 2;

// we use this when we reasign something

let result = 1 + 2 * ( 3 + 4);
console.log(result)
